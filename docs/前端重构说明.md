# 前端架构重构说明

## 重构目标

从旧的静态页面架构迁移到 **Vue 3 + TypeScript + Element Plus + Pinia** 现代 SPA 架构。

**文档版本**: v2.0
**最后更新**: 2026-07

---

## 技术栈

| 技术 | 用途 |
|---|---|
| Vue 3 | 前端框架 |
| TypeScript | 类型安全 |
| Vite | 构建工具 |
| Pinia | 状态管理 |
| Element Plus | UI 组件库（按需引入） |
| Vue Router | 路由管理 |
| EventSource | SSE 客户端 |

---

## 目录结构

```
datatrace-ui-designed/src/
├── main.ts                    # 应用入口
├── App.vue                    # 根组件（Sidebar + TopBar 导航布局）
├── router/index.ts            # 路由配置
├── stores/                    # Pinia Store
│   ├── credential.ts          # 凭据 Store
│   ├── datasource.ts          # 数据源 Store
│   └── task.ts                # 任务 Store（配置+监控统一）
├── api/                       # HTTP 请求封装
│   ├── credential.ts
│   ├── datasource.ts
│   └── task.ts
├── types/index.ts             # TypeScript 类型定义
├── utils/dialog.ts            # 对话框工具
├── assets/styles/design-tokens.css  # 设计令牌
├── components/                # 通用组件
│   ├── Sidebar.vue            # 侧边导航
│   ├── TopBar.vue             # 顶部栏
│   ├── CredentialModal/       # 凭据弹窗
│   ├── DataSourceModal/       # 数据源弹窗
│   └── TaskWizard/            # 4步任务配置向导
└── views/                     # 页面视图
    ├── Overview/OverviewView.vue
    ├── DataSource/DataSourceView.vue
    ├── Credential/CredentialView.vue
    ├── TaskConfig/TaskConfigView.vue
    └── TaskMonitor/TaskMonitorView.vue
```

---

## 路由

| 路径 | 页面 | 说明 |
|---|---|---|
| `/` | 首页 | 重定向到 `/task-monitor` |
| `/datasources` | DataSourceView | 数据源管理 |
| `/credentials` | CredentialView | 凭据管理 |
| `/task-config` | TaskConfigView | 任务配置 |
| `/task-monitor` | TaskMonitorView | 任务监控 |

---

## SSE 集成

前端通过 `useTaskStore` 管理 SSE 连接：

```typescript
function connectSSE(taskId: string) {
    const topics = `task:${taskId}:detail,task:${taskId}:progress,task:${taskId}:logs,task:${taskId}:table_status,task:${taskId}:step_status`
    const es = new EventSource(`/api/v1/sse?topics=${topics}`)

    es.addEventListener('task_detail', (e) => { /* 更新任务状态 */ })
    es.addEventListener('progress', (e) => { /* 更新进度 */ })
    es.addEventListener('log', (e) => { /* 追加日志 */ })
    es.addEventListener('table_status', (e) => { /* 更新表状态 */ })
    es.addEventListener('step_status', (e) => { /* 更新步骤状态 */ })
}
```

**生命周期**：页面进入建立连接，页面离开断开连接。

---

## 组件设计

| 组件 | 说明 |
|---|---|
| Sidebar | 侧边导航菜单 |
| TopBar | 顶部栏（标题+操作） |
| CredentialModal | 凭据新建/编辑弹窗 |
| DataSourceModal | 数据源新建/编辑弹窗 |
| TaskWizard | 4步任务配置向导（选源→选表→配置→确认） |

---

## 交互规范

| 场景 | 表现 |
|---|---|
| 成功操作 | Toast 绿色提示，3s 自动关闭 |
| 错误操作 | Toast 红色提示，5s 可手动关闭 |
| 按钮操作 | loading 状态，禁用点击 |
| 删除确认 | MessageBox 弹窗确认 |
| 弹窗确认 | Enter 键触发 |
| 弹窗取消 | Esc 键关闭 |
# 前端架构重构说明

## 重构目标

将原有的单页应用（SPA）架构重构为多页应用（MPA）架构，实现模块化和隔离，每个菜单独立页面和资源。

## 新的目录结构

```
static/
├── components/              # 通用组件（供所有模块调用）
│   ├── toast/              # Toast 通知组件
│   │   ├── toast.js
│   │   └── toast.css
│   ├── modal/              # Modal 对话框组件
│   │   └── modal.js
│   └── utils/              # 工具函数
│       ├── format.js       # 格式化工具
│       └── http.js         # HTTP 请求封装
│
├── modules/                 # 业务模块（相对隔离）
│   ├── datasource/         # 数据源管理模块
│   │   ├── index.js        # 模块入口 ⭐
│   │   ├── datasource.js   # 核心逻辑
│   │   ├── datasource.css  # 模块样式
│   │   └── sse.js          # SSE 连接管理
│   │
│   ├── task-config/        # 任务配置模块
│   │   ├── index.js        # 模块入口 ⭐
│   │   ├── task-config.js  # 核心逻辑
│   │   ├── task-config.css # 模块样式
│   │   └── wizard/         # 配置向导
│   │       ├── wizard.js   # 向导核心逻辑
│   │       ├── wizard.css  # 向导样式
│   │       └── steps/      # 向导步骤
│   │           ├── step1.js  # 步骤1：选择数据源
│   │           ├── step2.js  # 步骤2：选择表
│   │           ├── step3.js  # 步骤3：同步配置
│   │           └── step4.js  # 步骤4：确认配置
│   │
│   └── task-monitor/       # 任务监控模块
│       ├── index.js        # 模块入口 ⭐
│       ├── task-list.js    # 任务列表管理
│       ├── task-detail.js  # 任务详情管理
│       ├── task-control.js # 任务控制
│       ├── task-monitor.css# 模块样式
│       └── sse/            # SSE 连接
│           ├── progress.js # 进度 SSE
│           └── logs.js     # 日志 SSE
│
└── css/
    └── style.css           # 全局样式

templates/
├── layout/                 # 公共布局
│   ├── header.html         # 页面头部
│   ├── sidebar.html        # 侧边栏菜单
│   └── footer.html         # 页面底部
│
├── datasource/
│   └── index.html          # 数据源页面
│
├── task-config/
│   └── index.html          # 任务配置页面
│
└── task-monitor/
    └── index.html          # 任务监控页面
```

## 模块设计

### 1. 通用组件（Components）

所有模块共享的组件，提供基础功能：

- **Toast**: 消息通知组件
  - `Toast.success(message)` - 成功提示
  - `Toast.error(message)` - 错误提示
  - `Toast.warning(message)` - 警告提示
  - `Toast.info(message)` - 信息提示

- **Modal**: 对话框组件
  - `Modal.confirm(message, onConfirm, onCancel)` - 确认对话框

- **FormatUtils**: 格式化工具
  - `FormatUtils.formatDate(dateString)` - 格式化日期
  - `FormatUtils.formatSize(bytes)` - 格式化文件大小
  - `FormatUtils.formatNumber(num)` - 格式化数字
  - `FormatUtils.formatDuration(seconds)` - 格式化时间间隔

- **HttpUtils**: HTTP 请求封装
  - `HttpUtils.get(url)` - GET 请求
  - `HttpUtils.post(url, data)` - POST 请求
  - `HttpUtils.put(url, data)` - PUT 请求
  - `HttpUtils.delete(url)` - DELETE 请求

### 2. 业务模块（Modules）

每个模块包含三个文件：

- **index.js**: 模块入口文件
  - `init()` - 初始化模块
  - `destroy()` - 清理模块资源
  - `bindEvents()` - 绑定事件

- **[module].js**: 核心业务逻辑
  - 数据加载
  - 业务处理
  - UI 更新

- **[module].css**: 模块样式
  - 模块特定样式
  - 不影响其他模块

### 3. 页面模板（Templates）

每个页面独立的 HTML 文件：

- 引入公共布局（header、sidebar、footer）
- 引入模块样式
- 引入通用组件
- 引入模块脚本
- 调用模块初始化

## 路由配置

```go
// 页面路由
r.GET("/", pageCtrl.Index)                      // 首页（重定向到数据源）
r.GET("/datasources", pageCtrl.DataSource)     // 数据源页面
r.GET("/task-config", pageCtrl.TaskConfig)     // 任务配置页面
r.GET("/task-monitor", pageCtrl.TaskMonitor)   // 任务监控页面
r.GET("/task-monitor/:id", pageCtrl.TaskMonitorWithID) // 任务监控（带ID）
```

## 模块生命周期

### 初始化流程

1. 页面加载
2. 加载通用组件（Toast、Modal、Utils）
3. 加载模块脚本
4. 调用模块 `init()` 方法
5. 模块加载数据并渲染 UI

### 清理流程

1. 用户离开页面
2. 触发 `beforeunload` 事件
3. 调用模块 `destroy()` 方法
4. 关闭 SSE 连接
5. 清理事件监听器

## 优势

1. **模块隔离**: 每个模块独立，互不影响
2. **代码清晰**: 文件结构清晰，易于维护
3. **按需加载**: 只加载当前页面需要的资源
4. **易于扩展**: 新增模块只需创建对应目录和文件
5. **团队协作**: 不同开发者可以独立开发不同模块

## 重构总结

### 已删除的旧文件

以下旧文件已删除：

- `static/js/app.js` - 旧的通用工具
- `static/js/task.js` - 旧的任务逻辑
- `static/js/task-wizard-new.js` - 旧的任务向导
- `static/js/task-wizard/core.js` - 旧的向导核心
- `static/js/task-wizard/step1.js` - 旧的向导步骤1
- `static/js/task-wizard/step2.js` - 旧的向导步骤2
- `static/js/task-wizard/step3.js` - 旧的向导步骤3
- `static/js/task-wizard/step4.js` - 旧的向导步骤4
- `static/js/task-wizard/modals.js` - 旧的向导模态框
- `static/js/task-manage/core.js` - 旧的任务监控核心
- `static/js/task-manage/detail.js` - 旧的任务详情
- `static/js/task-manage/control.js` - 旧的任务控制
- `static/js/task-manage/ui.js` - 旧的任务 UI

### 新增文件列表

**通用组件（7 个文件）**:
- `static/components/toast/toast.js` - Toast 通知组件
- `static/components/toast/toast.css` - Toast 样式
- `static/components/modal/modal.js` - Modal 对话框组件
- `static/components/modal/modal.css` - Modal 样式
- `static/components/utils/format.js` - 格式化工具
- `static/components/utils/http.js` - HTTP 请求封装
- `static/components/utils/common.css` - 通用样式

**公共布局（3 个文件）**:
- `templates/layout/header.html` - 页面头部
- `templates/layout/sidebar.html` - 侧边栏菜单
- `templates/layout/footer.html` - 页面底部

**数据源模块（4 个文件）**:
- `static/modules/datasource/index.js` - 模块入口
- `static/modules/datasource/datasource.js` - 核心逻辑
- `static/modules/datasource/datasource.css` - 模块样式
- `static/modules/datasource/sse.js` - SSE 连接管理
- `templates/datasource/index.html` - 数据源页面

**任务配置模块（4 个文件）**:
- `static/modules/task-config/index.js` - 模块入口
- `static/modules/task-config/task-config.js` - 核心逻辑
- `static/modules/task-config/task-config.css` - 模块样式
- `templates/task-config/index.html` - 任务配置页面

**任务监控模块（8 个文件）**:
- `static/modules/task-monitor/index.js` - 模块入口
- `static/modules/task-monitor/task-list.js` - 任务列表管理
- `static/modules/task-monitor/task-detail.js` - 任务详情管理
- `static/modules/task-monitor/task-control.js` - 任务控制
- `static/modules/task-monitor/task-monitor.css` - 模块样式
- `static/modules/task-monitor/sse/progress.js` - 进度 SSE 连接
- `static/modules/task-monitor/sse/logs.js` - 日志 SSE 连接
- `templates/task-monitor/index.html` - 任务监控页面

**后端文件修改（2 个文件）**:
- `api/page_controller.go` - 更新页面控制器
- `routers/router.go` - 更新路由配置

**文档（1 个文件）**:
- `docs/前端重构说明.md` - 重构说明文档

### 文件统计

- 删除文件：13 个
- 新增文件：33 个
- 修改文件：2 个
- 总计：48 个文件变更

## 迁移说明

### 旧文件保留

以下文件保留但不再使用：

- `templates/layout/base.html` - 旧的单页应用（保留作为参考）
- `templates/datasource/list.html` - 旧的数据源列表页
- `templates/datasource/form.html` - 旧的数据源表单页
- `templates/task/list.html` - 旧的任务列表页
- `templates/task/wizard.html` - 旧的任务向导页

## 测试步骤

1. 启动应用
2. 访问 `http://localhost:8080/`（自动重定向到数据源页面）
3. 测试数据源管理功能
4. 点击侧边栏切换到任务配置页面
5. 测试任务配置功能
6. 点击侧边栏切换到任务监控页面
7. 测试任务监控功能

## 模块功能说明

### 数据源模块

**功能**：
- 数据源列表展示
- 新建/编辑/删除数据源
- 实时连接测试（SSE）
- 支持 MySQL 和 Elasticsearch

**文件说明**：
- `datasource.js` - 数据源 CRUD 操作
- `sse.js` - SSE 连接测试管理
- `index.js` - 模块初始化和清理

### 任务配置模块

**功能**：
- 任务列表展示
- 新建/查看/删除任务
- 四步配置向导

**配置向导步骤**：
1. **步骤1 - 选择数据源**：选择源数据源和目标数据源
2. **步骤2 - 选择表**：选择要同步的数据库和表
3. **步骤3 - 同步配置**：配置同步参数（批次大小、线程数等）
4. **步骤4 - 确认配置**：预览并确认所有配置

**文件说明**：
- `task-config.js` - 任务列表管理
- `wizard/wizard.js` - 向导核心逻辑（步骤切换、数据管理）
- `wizard/steps/step1.js` - 数据源选择逻辑
- `wizard/steps/step2.js` - 表选择逻辑
- `wizard/steps/step3.js` - 同步配置逻辑
- `wizard/steps/step4.js` - 配置确认逻辑
- `index.js` - 模块初始化

### 任务监控模块

**功能**：
- 任务列表展示（全量/增量分类）
- 任务详情查看
- 任务控制（启动/暂停/停止）
- 实时进度监控（SSE）
- 实时日志查看（SSE）

**文件说明**：
- `task-list.js` - 任务列表管理
- `task-detail.js` - 任务详情和进度/日志展示
- `task-control.js` - 任务控制操作
- `sse/progress.js` - 进度 SSE 连接管理
- `sse/logs.js` - 日志 SSE 连接管理
- `index.js` - 模块初始化和清理

## 后续优化

1. ✅ 完善任务配置向导功能（已完成）
2. 增强任务监控的实时更新（SSE）
3. 添加更多通用组件（如表格、表单等）
4. 优化样式和交互体验
5. 添加单元测试
6. 添加错误边界处理
7. 优化 SSE 重连机制


## 模块功能详细说明

### 数据源模块

**功能**：
- 数据源列表展示
- 新建/编辑/删除数据源
- 实时连接测试（SSE）
- 支持 MySQL 和 Elasticsearch

**文件说明**：
- `datasource.js` - 数据源 CRUD 操作、表单验证
- `sse.js` - SSE 连接测试管理、状态更新
- `index.js` - 模块初始化、事件绑定、资源清理

### 任务配置模块

**功能**：
- 任务列表展示
- 新建/查看/删除任务
- 四步配置向导

**配置向导步骤**：
1. **步骤1 - 选择数据源**：选择源数据源和目标数据源
2. **步骤2 - 选择表**：选择要同步的数据库和表（支持批量选择）
3. **步骤3 - 同步配置**：配置同步参数（批次大小、线程数、错误策略等）
4. **步骤4 - 确认配置**：预览并确认所有配置信息

**文件说明**：
- `task-config.js` - 任务列表管理、查看任务详情
- `wizard/wizard.js` - 向导核心逻辑（步骤切换、数据管理、提交配置）
- `wizard/steps/step1.js` - 数据源选择逻辑和 UI
- `wizard/steps/step2.js` - 表选择逻辑和 UI（支持全选/取消全选）
- `wizard/steps/step3.js` - 同步配置表单和验证
- `wizard/steps/step4.js` - 配置预览和确认
- `index.js` - 模块初始化

### 任务监控模块

**功能**：
- 任务列表展示（全量/增量分类）
- 任务详情查看
- 任务控制（启动/暂停/停止）
- 实时进度监控（SSE）
- 实时日志查看（SSE）

**文件说明**：
- `task-list.js` - 任务列表加载、渲染、选择
- `task-detail.js` - 任务详情、进度和日志展示
- `task-control.js` - 任务控制操作（启动/暂停/停止）
- `sse/progress.js` - 进度 SSE 连接管理（支持多步骤）
- `sse/logs.js` - 日志 SSE 连接管理
- `index.js` - 模块初始化和资源清理

## API 接口说明

### 数据源接口
- `GET /api/v1/datasources` - 获取数据源列表
- `POST /api/v1/datasources` - 创建数据源
- `GET /api/v1/datasources/:id` - 获取数据源详情
- `PUT /api/v1/datasources/:id` - 更新数据源
- `DELETE /api/v1/datasources/:id` - 删除数据源
- `POST /api/v1/datasources/test` - 测试连接
- `GET /api/v1/datasources/test/stream` - SSE 测试流
- `GET /api/v1/datasources/:id/tables` - 获取数据库和表列表

### 任务接口
- `GET /api/v1/tasks` - 获取任务列表
- `POST /api/v1/tasks` - 创建任务
- `GET /api/v1/tasks/:id` - 获取任务详情
- `PUT /api/v1/tasks/:id/config` - 更新任务配置
- `DELETE /api/v1/tasks/:id` - 删除任务
- `POST /api/v1/tasks/:id/start` - 启动任务
- `POST /api/v1/tasks/:id/pause` - 暂停任务
- `POST /api/v1/tasks/:id/stop` - 停止任务
- `GET /api/v1/tasks/:id/progress` - 获取任务进度
- `GET /api/v1/tasks/:id/logs` - 获取任务日志
- `GET /api/v1/tasks/:id/stream/initialize` - SSE 初始化进度流
- `GET /api/v1/tasks/:id/stream/sync` - SSE 同步进度流
- `GET /api/v1/tasks/:id/stream/incremental` - SSE 增量进度流
- `GET /api/v1/tasks/:id/stream/logs` - SSE 日志流
