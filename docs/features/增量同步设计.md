# 增量同步设计

## 文档说明

本文档描述 DataTrace 基于 MySQL Binlog 的 CDC 增量同步机制。

**文档版本**: v4.0
**最后更新**: 2026-07

---

## 设计目标

1. **实时数据同步**：捕获源库 INSERT/UPDATE/DELETE 变更
2. **数据零丢失**：全量+增量无缝衔接
3. **断点续传**：支持中断后从上次位置继续
4. **多目标扇出**：一个 Binlog → N 个目标

---

## 核心组件

| 组件 | 文件 | 职责 |
|---|---|---|
| IncrementalSync | `incremental_sync.go` | 增量总控 |
| BinlogListener | `binlog_listener.go` | Binlog 监听（canal） |
| BinlogQueue | `binlog_queue.go` | 事件队列（内存/Redis） |
| IncrementalConsumer | `incremental_consumer.go` | 事件消费 → 写入目标 |
| ConsumerEventHandler | `incremental_consumer_event_handler.go` | INSERT/UPDATE/DELETE 处理 |
| ConsumerMapper | `incremental_consumer_mapper.go` | 源→目标映射 |
| IncrementalStatsService | `incremental_stats_service.go` | 统计服务 |

---

## 启动流程

```
IncrementalSync.Start()
  │
  ├── 1. 检查 binlog_format=ROW
  ├── 2. 捕获快照点（File+Position / GTID）
  ├── 3. 创建 BinlogQueue × len(targets)（扇出队列）
  ├── 4. 创建 IncrementalConsumer × len(targets)
  ├── 5. FullSyncStarter.Start() → 先全量
  ├── 6. Consumer.Start() × N（异步消费）
  └── 7. BinlogListener.Start() → 从快照点监听
```

---

## 事件处理

```
Queue.Pull() → BinlogEvent
  → Mapper.Map(sourceDB, sourceTable) → (targetDB, targetTable)
  → EventHandler.Handle(event)
    → INSERT: BatchInsert
    → UPDATE: SET + WHERE
    → DELETE: DELETE WHERE
  → MySQLWriter 执行
  → Queue.Ack(offset)
  → StatsSvc.UpdateStats()
```

---

## 扇出模式

```
源 MySQL ──→ BinlogListener
                ├── Queue_1 → Consumer_1 → 目标 MySQL_1
                ├── Queue_2 → Consumer_2 → 目标 MySQL_2
                └── Queue_N → Consumer_N → 目标 MySQL_N
```

---

## BinlogQueue 实现

| 实现 | 特点 |
|---|---|
| MemoryQueue | 进程内 channel，重启丢失 |
| RedisQueue | Redis List，持久化，断点续传 |

---

## 统计推送

```
StatsSvc.UpdateStats() → EventBus.Publish("incremental.stats") → SSEHub → 前端
```

SSE Topic：`task:{id}:incremental`

---

## 断点续传

- 快照点存储在 `sync_tasks` 表：
  - `snapshot_binlog_file` / `snapshot_binlog_pos`
  - `current_binlog_file` / `current_binlog_pos`
- 重启后从存储的位点继续监听
# 增量同步设计（CDC 方案）

## 📋 文档说明

本文档详细描述 DataTrace 系统基于 MySQL Binlog 的 CDC（Change Data Capture）增量同步机制。

**文档版本**: v3.0  
**最后更新**: 2026-02-27  
**技术栈**: Go + Gin + GORM + Redis + go-mysql  
**实现状态**: 设计阶段

---

## 🎯 设计目标

### 核心需求

1. **实时数据同步**: 捕获源库的实时变更（INSERT/UPDATE/DELETE）
2. **数据零丢失**: 全量+增量无缝衔接，保证数据完整性
3. **断点续传**: 支持中断后从上次位置继续
4. **高性能**: 异步处理，不影响源库性能
5. **可观测**: 实时监控延迟、速度、错误

---

## 🏗️ 核心架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    DataTrace 应用                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐         ┌──────────────┐             │
│  │ 全量同步引擎  │         │ Binlog监听器  │             │
│  │              │         │ (go-mysql)   │             │
│  │ - 读取快照    │         │ - 解析事件    │             │
│  │ - 批量写入    │         │ - 写入队列    │             │
│  └──────┬───────┘         └──────┬───────┘             │
│         │                        │                      │
│         ↓                        ↓                      │
│  ┌──────────────────────────────────────┐              │
│  │         增量消费引擎                  │              │
│  │  - 从 Redis Stream 读取               │              │
│  │  - 应用到目标库                       │              │
│  │  - ACK 确认                           │              │
│  └──────────────────────────────────────┘              │
│                                                          │
└─────────────────────────────────────────────────────────┘
         │                    │                    │
         ↓                    ↓                    ↓
    ┌────────┐          ┌─────────┐         ┌──────────┐
    │ MySQL  │          │  Redis  │         │ 目标MySQL │
    │ (配置) │          │ (队列)  │         │          │
    └────────┘          └─────────┘         └──────────┘
```

### 技术选型

**Binlog 解析**：
- `go-mysql` - 纯 Go 实现，性能好，社区活跃

**消息队列**：
- **Redis Stream**（推荐）- 持久化、消费组、轻量级
- **内存 Channel** - 简单快速，但重启丢失
- **本地文件队列** - 无外部依赖，但性能一般

---

## 🔄 数据零丢失方案

### 核心问题

```
T0: 开始同步，记录 Binlog 位置 (file=mysql-bin.000001, pos=1000)
   ↓
T1-T5: 全量同步进行中（可能需要几小时）
   ├─ 期间源库持续有新数据写入
   ├─ Binlog 继续增长 (pos=1000 → 5000)
   └─ 这些增量数据如何处理？❓
   ↓
T6: 全量同步完成
   ↓
T7: 开始增量同步
   └─ 如何保证 T1-T6 期间的数据不丢失？❓
```

### 解决方案：全量+增量并行

```
┌─────────────────────────────────────────┐
│ 阶段1: 记录起始位置（快照点）            │
│ - 获取当前 Binlog 位置                   │
│ - 开启事务（保证一致性）                 │
│ - Snapshot Point: file=xxx, pos=1000    │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 阶段2: 并行执行                          │
│                                         │
│  [全量同步线程]        [增量监听线程]     │
│       ↓                      ↓          │
│  读取存量数据          监听 Binlog       │
│  (T0 时刻的快照)       (从 T0 开始)      │
│       ↓                      ↓          │
│  写入目标库            写入 Redis Stream │
│                       (暂不应用)         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 阶段3: 全量完成，切换增量                 │
│ - 全量同步完成                           │
│ - 开始消费 Redis Stream                  │
│ - 应用 T0 之后的所有变更                  │
└─────────────────────────────────────────┘
```

### 一致性保证

#### 使用事务快照

```sql
-- 1. 开启一致性快照
START TRANSACTION WITH CONSISTENT SNAPSHOT;

-- 2. 获取 Binlog 位置
SHOW MASTER STATUS;
-- 结果: file=mysql-bin.000001, position=1000

-- 3. 执行全量查询
SELECT * FROM table1;
SELECT * FROM table2;

-- 4. 提交事务
COMMIT;
```

**优点**：保证全量数据和 Binlog 位置的一致性

---

## 📊 数据模型设计

### MySQL 表结构扩展

```sql
-- sync_tasks 表添加增量同步字段
ALTER TABLE sync_tasks 
ADD COLUMN incremental_enabled BOOLEAN DEFAULT FALSE COMMENT '是否启用增量同步',
ADD COLUMN queue_type VARCHAR(20) DEFAULT 'memory' COMMENT '队列类型: memory/redis/file',
ADD COLUMN snapshot_binlog_file VARCHAR(100) COMMENT '快照点Binlog文件',
ADD COLUMN snapshot_binlog_pos INT UNSIGNED COMMENT '快照点Binlog位置',
ADD COLUMN snapshot_gtid TEXT COMMENT '快照点GTID',
ADD COLUMN current_binlog_file VARCHAR(100) COMMENT '当前Binlog文件',
ADD COLUMN current_binlog_pos INT UNSIGNED COMMENT '当前Binlog位置',
ADD COLUMN full_sync_completed BOOLEAN DEFAULT FALSE COMMENT '全量同步是否完成',
ADD COLUMN incremental_lag INT DEFAULT 0 COMMENT '增量延迟(秒)',
ADD COLUMN incremental_events_total BIGINT DEFAULT 0 COMMENT '增量事件总数',
ADD COLUMN incremental_events_applied BIGINT DEFAULT 0 COMMENT '已应用事件数';
```

### Go 数据结构

```go
// 增量同步配置
type IncrementalConfig struct {
    Enabled    bool   `json:"enabled"`     // 是否启用
    QueueType  string `json:"queue_type"`  // memory/redis/file
    BufferSize int    `json:"buffer_size"` // 缓冲区大小
    
    // Redis 配置（queue_type=redis 时需要）
    RedisAddr     string `json:"redis_addr,omitempty"`
    RedisPassword string `json:"redis_password,omitempty"`
    RedisDB       int    `json:"redis_db,omitempty"`
    
    // 文件队列配置（queue_type=file 时需要）
    FilePath string `json:"file_path,omitempty"`
}

// Binlog 事件
type BinlogEvent struct {
    ID        string                 `json:"id"`        // 事件ID
    Type      string                 `json:"type"`      // INSERT/UPDATE/DELETE
    Database  string                 `json:"database"`  
    Table     string                 `json:"table"`     
    Timestamp uint32                 `json:"timestamp"` 
    Data      map[string]interface{} `json:"data"`      // 新数据
    OldData   map[string]interface{} `json:"old_data"`  // 旧数据(UPDATE)
    
    // Binlog 位置信息
    BinlogFile string `json:"binlog_file"`
    BinlogPos  uint32 `json:"binlog_pos"`
}

// 增量同步状态
type IncrementalState struct {
    TaskID              string
    Phase               string // init/full/incremental
    SnapshotFile        string
    SnapshotPos         uint32
    SnapshotGTID        string
    CurrentFile         string
    CurrentPos          uint32
    FullSyncCompleted   bool
    IncrementalLag      int64  // 延迟（秒）
    EventsTotal         int64  // 总事件数
    EventsApplied       int64  // 已应用事件数
}
```

### Redis Stream 数据结构

```
Stream Key: binlog:{taskID}

消息格式:
{
  "id": "1234567890-0",
  "type": "INSERT",
  "database": "test_db",
  "table": "users",
  "timestamp": 1234567890,
  "data": {
    "id": 1,
    "name": "张三",
    "created_at": "2026-02-27 10:00:00"
  },
  "binlog_file": "mysql-bin.000001",
  "binlog_pos": 1234
}

消费组:
Group: sync-group-{taskID}
Consumer: worker-1, worker-2, ...
```

---

## 🔧 执行流程

### 完整流程图

```
用户启动增量同步任务
   ↓
┌──────────────────────────────────────┐
│ 1. 初始化阶段                         │
│ - 检查源库 Binlog 是否开启            │
│ - 检查用户权限 (REPLICATION SLAVE)    │
│ - 创建队列（Redis Stream/Channel）    │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 2. 记录快照点                         │
│ - START TRANSACTION WITH CONSISTENT   │
│ - SHOW MASTER STATUS                  │
│ - 记录: file=xxx, pos=1000            │
│ - 保存到数据库                        │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 3. 启动 Binlog 监听（异步）           │
│ - 从 pos=1000 开始监听                │
│ - 解析事件 → 写入队列                 │
│ - 持续运行，不阻塞                     │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 4. 执行全量同步                       │
│ - 读取快照时刻的数据                  │
│ - 批量写入目标库                      │
│ - 显示进度: 50/100 表                 │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 5. 全量完成，切换增量                 │
│ - 标记 full_sync_completed = true     │
│ - 启动增量消费线程                    │
│ - 从队列读取并应用事件                │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 6. 持续增量同步                       │
│ - 实时消费 Binlog 事件                │
│ - 应用到目标库                        │
│ - 监控延迟和速度                      │
│ - 定期保存 Binlog 位置                │
└──────────────────────────────────────┘
```

### 核心代码流程

```go
// 启动增量同步
func (s *IncrementalSync) Start() error {
    // 1. 记录快照点
    snapshot, err := s.captureSnapshot()
    if err != nil {
        return err
    }
    
    // 2. 启动 Binlog 监听（异步）
    queue := s.createQueue() // Redis Stream 或 Channel
    go s.startBinlogListener(snapshot, queue)
    
    // 3. 执行全量同步
    err = s.fullSync(snapshot)
    if err != nil {
        return err
    }
    
    // 4. 标记全量完成
    s.markFullSyncCompleted()
    
    // 5. 启动增量消费
    go s.consumeIncrementalEvents(queue)
    
    return nil
}

// 捕获快照点
func (s *IncrementalSync) captureSnapshot() (*Snapshot, error) {
    // 开启事务
    tx, _ := s.sourceDB.Begin()
    defer tx.Rollback()
    
    // 获取 Binlog 位置
    var file string
    var pos uint32
    tx.QueryRow("SHOW MASTER STATUS").Scan(&file, &pos, ...)
    
    return &Snapshot{
        File:      file,
        Position:  pos,
        Timestamp: time.Now(),
    }, nil
}

// Binlog 监听器
func (s *IncrementalSync) startBinlogListener(snapshot *Snapshot, queue BinlogQueue) {
    syncer := replication.NewBinlogSyncer(...)
    syncer.StartSync(mysql.Position{
        Name: snapshot.File,
        Pos:  snapshot.Position,
    })
    
    for {
        event, _ := syncer.GetEvent()
        binlogEvent := s.parseEvent(event)
        if binlogEvent != nil {
            queue.Push(binlogEvent) // 写入队列
        }
    }
}

// 增量消费
func (s *IncrementalSync) consumeIncrementalEvents(queue BinlogQueue) {
    for {
        event, _ := queue.Pop()
        
        // 应用变更
        err := s.applyEvent(event)
        if err != nil {
            log.Error(err)
            continue
        }
        
        // ACK 确认
        queue.Ack(event.ID)
        
        // 更新位置
        s.updatePosition(event.BinlogFile, event.BinlogPos)
    }
}

// 应用事件
func (s *IncrementalSync) applyEvent(event *BinlogEvent) error {
    switch event.Type {
    case "INSERT":
        return s.handleInsert(event)
    case "UPDATE":
        return s.handleUpdate(event)
    case "DELETE":
        return s.handleDelete(event)
    }
    return nil
}
```

---

## 🎯 队列方案对比

### 方案 1：内存队列（Channel）

**优点**：
- ✅ 无需外部依赖
- ✅ 性能最好
- ✅ 实现简单

**缺点**：
- ❌ 重启丢失数据
- ❌ 无法断点续传
- ❌ 不适合长时间全量同步

**适用场景**：
- 开发测试环境
- 数据量小，全量同步很快（几分钟内）

### 方案 2：Redis Stream（推荐）

**优点**：
- ✅ 数据持久化（AOF/RDB）
- ✅ 支持断点续传
- ✅ 消费组支持多消费者
- ✅ ACK 机制保证至少一次消费
- ✅ 轻量级，易部署

**缺点**：
- ❌ 需要部署 Redis
- ❌ 单机性能有限

**适用场景**：
- 生产环境
- 数据量大，全量同步时间长
- 需要高可靠性

### 方案 3：本地文件队列

**优点**：
- ✅ 无需外部依赖
- ✅ 数据持久化

**缺点**：
- ❌ 性能一般
- ❌ 无法多消费者
- ❌ 文件管理复杂

**适用场景**：
- 无法部署 Redis 的环境
- 单机部署

---

## 📈 监控指标

### 关键指标

1. **增量延迟**：
   - 当前 Binlog 位置 - 源库最新位置
   - 单位：秒

2. **消费速度**：
   - 每秒处理的事件数
   - 单位：events/s

3. **队列积压**：
   - 队列中未消费的事件数
   - 单位：条

4. **错误率**：
   - 应用失败的事件比例
   - 单位：%

### 监控实现

```go
// 增量同步监控
type IncrementalMonitor struct {
    TaskID            string
    CurrentPos        uint32
    LatestPos         uint32
    Lag               int64  // 延迟（秒）
    EventsPerSecond   int64  // 消费速度
    QueueSize         int64  // 队列积压
    ErrorRate         float64 // 错误率
}

// 计算延迟
func (m *IncrementalMonitor) CalculateLag() int64 {
    // 通过 Binlog 位置差异估算延迟
    posDiff := m.LatestPos - m.CurrentPos
    // 假设每秒产生 1000 个事件
    return int64(posDiff / 1000)
}
```

---

## 🚀 实施计划

### Phase 1：基础 CDC 功能

- ⬜ 集成 `go-mysql` 库
- ⬜ 实现 Binlog 监听器
- ⬜ 实现内存队列（Channel）
- ⬜ 实现事件解析和应用
- ⬜ 实现快照点捕获
- ⬜ 实现全量+增量并行

### Phase 2：Redis 持久化

- ⬜ 添加 Redis 配置
- ⬜ 实现 Redis Stream 队列
- ⬜ 实现消费组
- ⬜ 实现 ACK 机制
- ⬜ 实现断点续传

### Phase 3：监控和优化

- ⬜ 实现延迟监控
- ⬜ 实现速度监控
- ⬜ 实现错误告警
- ⬜ 批量写入优化
- ⬜ 并发消费优化

### Phase 4：高级功能

- ⬜ 支持 DDL 同步
- ⬜ 支持数据过滤
- ⬜ 支持数据转换
- ⬜ 支持冲突解决
- ⬜ 支持 GTID 模式

---

## 📚 相关文档

- [任务调度与断点续传](./任务调度与断点续传.md) - 任务调度机制
- [系统设计文档](../requirements.md) - 完整系统设计
- [数据库设计](../数据库设计.md) - 数据库表结构

---

## 🔗 参考资料

- [go-mysql](https://github.com/go-mysql-org/go-mysql) - MySQL Binlog 解析库
- [Redis Streams](https://redis.io/docs/data-types/streams/) - Redis 流式数据结构
- [MySQL Binlog](https://dev.mysql.com/doc/refman/8.0/en/binary-log.html) - MySQL 二进制日志
- [阿里云 DTS](https://help.aliyun.com/document_detail/26592.html) - 数据传输服务
- [Canal](https://github.com/alibaba/canal) - 阿里巴巴开源 CDC 工具

---

**文档维护**: DataTrace 开发团队  
**最后更新**: 2026-02-27
