# 重构后架构说明

## 文档说明

本文档说明 DataTrace 系统重构后的当前架构，作为各模块的最终参考。

**文档版本**: v2.0
**最后更新**: 2026-07

---

## 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│  前端：Vue 3 + TypeScript + Element Plus + Pinia             │
│  datatrace-ui-designed/                                      │
└─────────────────────────────────────────────────────────────┘
                    ↕ HTTP REST + SSE
┌─────────────────────────────────────────────────────────────┐
│  后端：Go + Gin + GORM + MySQL + Redis                       │
│  datatrace-server/                                           │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  API Layer (api/)                                     │   │
│  │  CredentialAPI / DataSourceAPI / TaskAPI / TaskCtrl   │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Service Layer (services/)                            │   │
│  │  基础管理: Credential / DataSource / Task              │   │
│  │  全量同步: SyncEngine / MySQLReader / MySQLWriter      │   │
│  │  增量同步: BinlogListener / BinlogQueue / Consumer     │   │
│  │  横切服务: SSEHub / EventBus / CacheStore              │   │
│  │  辅助服务: HealthCheck / TaskLog / TableStatus         │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Infrastructure                                       │   │
│  │  MySQL (元数据) / Redis (缓存/队列/统计)               │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 重构要点

### 1. 依赖注入

- 使用 `AppContainer` 统一管理所有 Service 生命周期
- 禁止全局变量和 `sync.Once` 单例
- 初始化入口：`app.NewAppContainer(cfg, db, redisClient)`

### 2. 事件驱动推送

- 所有状态变更通过 `EventBus.Publish()` → `SSEHub` → 前端
- 7 个桥接注册：
  - `task.progress` → `task:{id}:progress`
  - `task.detail` → `task:{id}:detail`
  - `task.log` → `task:{id}:logs`
  - `task.table_status` → `task:{id}:table_status`
  - `task.step_status` → `task:{id}:step_status`
  - `incremental.stats` → `task:{id}:incremental`
  - `health.check` → `health:all`
- **禁止**轮询/ticker 兜底

### 3. 缓存降级

- `CacheStore` 接口支持 Memory/Redis 两种实现
- Redis 不可用时自动降级到内存缓存
- 统计信息使用 Redis Hash 存储

### 4. 多目标同步

- 任务支持多个目标数据源
- `TaskTargetSource` 关联表管理多目标关系
- Binlog 事件扇出到 N 个 Queue → N 个 Consumer

### 5. 任务日志持久化

- 每个任务独立日志文件：`logs/tasks/{taskID}.log`
- 通过 `TaskLogService` 写入，同时 EventBus 推送 SSE
- 任务启动时自动清空旧日志

### 6. 表/步骤状态管理

- `TaskTableStatusService` 管理表同步状态和步骤状态
- 状态变更通过 EventBus → SSEHub 实时推送
- 支持 Redis 持久化或内存存储

---

## 目录结构

```
datatrace-server/
├── main.go                    # 启动入口
├── app/container.go           # 依赖注入容器
├── common/
│   ├── logger.go              # 日志系统
│   └── response.go            # 统一响应
├── config/config.go           # 配置加载
├── database/
│   ├── mysql.go               # GORM 初始化
│   └── redis.go               # Redis 初始化
├── models/                    # 数据模型（4个表）
├── api/                       # HTTP Handler（4个文件）
├── routers/router.go          # 路由注册
├── services/                  # 业务逻辑（20+文件）
├── utils/crypto.go            # AES 加密
├── migrations/                # SQL 迁移
└── logs/                      # 日志目录

datatrace-ui-designed/src/
├── main.ts                    # 入口
├── App.vue                    # 根组件
├── router/index.ts            # 路由
├── stores/                    # Pinia Store（3个）
├── api/                       # HTTP 请求（3个）
├── types/index.ts             # 类型定义
├── components/                # 组件
└── views/                     # 页面（5个）
```

---

## 模块状态

| 模块 | 状态 | 说明 |
|---|---|---|
| 凭据管理 | ✅ 已完成 | CRUD + 加密 + 测试连接 |
| 数据源管理 | ✅ 已完成 | CRUD + 连接测试 + 健康检查 + 库表查询 |
| 任务管理 | ✅ 已完成 | CRUD + 配置读写 + 统计 |
| 任务控制 | ✅ 已完成 | 启停/暂停/恢复 + 进度/日志/状态 |
| 全量同步 | ✅ 已完成 | SyncEngine + Reader/Writer + 拓扑排序 |
| 增量同步 | ✅ 已完成 | BinlogListener + Queue + Consumer |
| SSE 推送 | ✅ 已完成 | 统一 Hub + 7 个桥接 |
| 任务日志 | ✅ 已完成 | 文件持久化 + SSE 推送 |
| 表/步骤状态 | ✅ 已完成 | Redis/内存 + SSE 推送 |
| 健康检查 | ✅ 已完成 | 定时检查 + SSE 推送 |
| 前端 UI | ✅ 已完成 | 5 个页面 + 3 个弹窗 + SSE 集成 |
# 重构后架构说明

> 更新时间：2024年
> 
> 本文档说明了去掉进度表和配置表后的新架构设计

## 📊 架构变更概述

### 变更原因

1. **简化架构** - 进度表和配置表增加了系统复杂度
2. **提升性能** - 全量同步不需要频繁写数据库
3. **明确职责** - 配置、进度、统计分别存储在最合适的地方

### 核心变更

- ❌ 删除 `task_unit_configs` 表
- ❌ 删除 `task_unit_runtimes` 表
- ✅ 配置详情存储到 Redis
- ✅ 运行时进度维护在内存
- ✅ 增量统计存储到 Redis

---

## 🏗️ 数据存储架构

### 1. MySQL - 基础配置和最终状态

**存储内容**：
- 数据源信息（`data_sources`）
- 任务基本信息（`sync_tasks`）
- 任务最终状态（`is_running`, `current_step`）

**表结构**：

```sql
-- 数据源表
CREATE TABLE `data_sources` (
    `id` VARCHAR(36) PRIMARY KEY,
    `name` VARCHAR(100) NOT NULL,
    `type` VARCHAR(20) NOT NULL,
    `host` VARCHAR(255) NOT NULL,
    `port` INT NOT NULL,
    `username` VARCHAR(100) NOT NULL,
    `password` VARCHAR(255) NOT NULL,
    `database_name` VARCHAR(100),
    `created_at` TIMESTAMP,
    `updated_at` TIMESTAMP
);

-- 同步任务表
CREATE TABLE `sync_tasks` (
    `id` VARCHAR(36) PRIMARY KEY,
    `name` VARCHAR(100) NOT NULL,
    `source_id` VARCHAR(36),
    `target_id` VARCHAR(36),
    `source_type` VARCHAR(20) NOT NULL,
    `target_type` VARCHAR(20) NOT NULL,
    `config` TEXT NOT NULL,              -- JSON格式的基本配置
    `status` VARCHAR(20) NOT NULL,       -- idle/configured
    `is_running` BOOLEAN DEFAULT FALSE,  -- 是否正在运行
    `sync_mode` VARCHAR(20) NOT NULL,    -- full/incremental
    `current_step` VARCHAR(50),          -- initialize/sync_data/completed/incremental
    `queue_type` VARCHAR(20),            -- memory/redis
    `created_at` TIMESTAMP,
    `updated_at` TIMESTAMP
);
```

---

### 2. Redis - 详情配置和增量统计

**存储内容**：

#### 2.1 任务配置详情
```
Key: task:config:{taskID}
Value: JSON
{
  "source_id": "xxx",
  "target_id": "xxx",
  "selected_databases": [
    {
      "database": "target_db",
      "source_database": "source_db",
      "is_database_modified": false,
      "tables": [
        {
          "source_table": "users",
          "target_table": "users",
          "is_modified": false
        }
      ]
    }
  ],
  "sync_config": {
    "batch_size": 1000,
    "thread_count": 4,
    "sync_mode": "full",
    "error_strategy": "pause",
    "table_exists_strategy": "drop"
  }
}
```

#### 2.2 增量同步统计
```
Key: task:stats:{taskID}
Value: Hash
{
  "db1.table1": {
    "insert_count": 100,
    "update_count": 50,
    "delete_count": 10,
    "last_update": "2024-01-01 12:00:00"
  },
  "db1.table2": {...}
}
```

---

### 3. 内存 - 运行时进度

**存储内容**：

```go
type TaskProgressData struct {
    TaskID      string
    Units       map[string]*TaskUnit  // unitName -> unit
    StartTime   time.Time
    CurrentStep string
}

type TaskUnit struct {
    UnitName         string
    Status           string    // pending/running/completed/failed/initialized
    TotalRecords     int64
    ProcessedRecords int64
    ErrorMessage     string
    StartedAt        *time.Time
    UpdatedAt        time.Time
}
```

**特点**：
- 任务启动时初始化
- 任务完成后清空
- 不持久化到数据库

---

## 🔄 同步模式对比

### 全量同步模式

```
启动
  ↓
从 Redis 读取配置（task:config:{taskID}）
  ↓
初始化内存进度（TaskProgressManager）
  ↓
外键分析和排序
  ↓
阶段1：初始化（创建数据库和表结构）
  ├─ 更新内存状态：initialized
  └─ SSE 推送进度
  ↓
阶段2：数据同步（多Worker并发）
  ├─ 更新内存进度：running → completed
  └─ SSE 推送进度
  ↓
完成
  ├─ 更新 MySQL：is_running=false, current_step=completed
  ├─ 清空内存进度
  └─ SSE 推送最终状态
```

**数据流**：
- 配置：Redis → 内存
- 进度：内存 → SSE → 前端
- 状态：内存 → MySQL（仅最终状态）

---

### 增量同步模式

```
启动
  ↓
捕获 Binlog 快照点
  ↓
┌─────────────────────┬─────────────────────┐
│   线程1：Binlog监听  │   线程2：全量同步    │
│                     │                     │
│  监听 Binlog 事件    │  从 Redis 读取配置   │
│       ↓             │       ↓             │
│  写入 Redis 队列     │  初始化内存进度      │
│  (持续后台运行)      │       ↓             │
│                     │  阶段1：初始化       │
│                     │       ↓             │
│                     │  阶段2：数据同步     │
│                     │       ↓             │
│                     │  SSE 推送进度        │
└─────────────────────┴─────────────────────┘
              ↓
        全量同步完成
              ↓
    更新 MySQL：current_step=incremental
              ↓
        启动增量消费
              ↓
    从 Redis 队列读取 Binlog 事件
              ↓
    应用到目标库
              ↓
    更新 Redis 统计（task:stats:{taskID}）
              ↓
    SSE 推送统计信息
```

**数据流**：
- 配置：Redis → 内存
- 全量进度：内存 → SSE → 前端
- Binlog事件：Binlog → Redis队列
- 增量统计：Redis → SSE → 前端
- 状态：内存 → MySQL（仅最终状态）

---

## 📡 SSE 推送机制

### 全量同步阶段

**推送内容**：
```json
{
  "task_id": "xxx",
  "sync_mode": "full",
  "current_step": "sync_data",
  "overall_progress": 45.5,
  "sync_speed": 1000,
  "elapsed_time": "00:05:30",
  "estimated_time": "00:06:30",
  "total_tables": 10,
  "completed_tables": 4,
  "total_records": 100000,
  "processed_records": 45500
}
```

**数据来源**：内存（TaskProgressManager）

---

### 增量消费阶段

**推送内容**：
```json
{
  "task_id": "xxx",
  "sync_mode": "incremental",
  "current_step": "incremental",
  "table_stats": [
    {
      "table_name": "db1.users",
      "insert_count": 100,
      "update_count": 50,
      "delete_count": 10,
      "last_update": "2024-01-01 12:00:00"
    }
  ]
}
```

**数据来源**：Redis（task:stats:{taskID}）

---

## 🔧 服务启动流程

```
1. 加载配置文件（config.yaml）
   ↓
2. 初始化 MySQL 连接
   ├─ 自动迁移表结构（data_sources, sync_tasks）
   └─ 不再创建 task_unit_configs 和 task_unit_runtimes
   ↓
3. 初始化 Redis 连接
   ↓
4. 加载所有已配置任务到 Redis
   ├─ 查询 MySQL：status='configured'
   ├─ 解析 config 字段
   └─ 写入 Redis：task:config:{taskID}
   ↓
5. 启动 HTTP 服务
```

---

## 🎯 配置修改流程

```
前端提交配置
  ↓
API 接收请求
  ↓
验证配置
  ↓
更新 MySQL
  ├─ sync_tasks.config（JSON）
  ├─ sync_tasks.status = 'configured'
  └─ sync_tasks.current_step = ''
  ↓
删除 Redis 旧配置
  └─ DEL task:config:{taskID}
  ↓
重新加载到 Redis
  └─ SET task:config:{taskID} {配置JSON}
  ↓
返回成功
```

---

## 📈 进度查询流程

### 全量同步进度查询

```
前端请求 /api/tasks/{id}/progress
  ↓
查询 MySQL 获取任务基本信息
  ├─ sync_mode
  └─ current_step
  ↓
从内存读取进度（TaskProgressManager）
  ├─ 统计各状态表数
  ├─ 计算总体进度
  ├─ 计算同步速度
  └─ 估算剩余时间
  ↓
返回 JSON
```

---

### 增量同步进度查询

```
前端请求 /api/tasks/{id}/progress
  ↓
查询 MySQL 获取任务基本信息
  ├─ sync_mode = 'incremental'
  └─ current_step = 'incremental'
  ↓
从 Redis 读取统计（task:stats:{taskID}）
  └─ 每个表的 insert/update/delete 数量
  ↓
返回 JSON
```

---

## 🗑️ 任务删除流程

```
前端请求删除任务
  ↓
检查任务状态
  └─ is_running = true → 拒绝删除
  ↓
清理 Redis 配置
  └─ DEL task:config:{taskID}
  ↓
清理 Redis 统计
  └─ DEL task:stats:{taskID}
  ↓
清理内存进度
  └─ TaskProgressManager.ClearTask(taskID)
  ↓
删除 MySQL 记录
  └─ DELETE FROM sync_tasks WHERE id = ?
  ↓
返回成功
```

---

## 💡 设计优势

### 1. 性能提升
- 全量同步无数据库IO，完全在内存
- 减少了数据库写入压力
- 提高了同步速度

### 2. 架构简化
- 删除了2个表，减少了表关联
- 代码更清晰，逻辑更简单
- 维护成本降低

### 3. 职责明确
- MySQL：持久化核心配置
- Redis：缓存详情配置和统计
- 内存：临时进度数据

### 4. 灵活扩展
- Redis配置可以支持更复杂的映射关系
- 内存进度结构可以灵活调整
- 不受数据库表结构限制

---

## ⚠️ 注意事项

### 1. Redis 依赖
- 系统依赖 Redis 存储配置
- Redis 故障时可以从 MySQL 降级读取基本配置
- 建议配置 Redis 持久化（RDB + AOF）

### 2. 内存管理
- 任务完成后及时清理内存进度
- 避免内存泄漏
- 大量并发任务时注意内存使用

### 3. 数据一致性
- 配置修改时先更新 MySQL，再更新 Redis
- 服务重启时从 MySQL 重建 Redis 配置
- 增量统计数据仅在 Redis，不持久化到 MySQL

---

## 🔄 迁移指南

### 从旧版本升级

1. **备份数据**
   ```sql
   -- 备份旧表数据（如需要）
   CREATE TABLE task_unit_configs_backup AS SELECT * FROM task_unit_configs;
   CREATE TABLE task_unit_runtimes_backup AS SELECT * FROM task_unit_runtimes;
   ```

2. **删除旧表**
   ```sql
   DROP TABLE IF EXISTS task_unit_runtimes;
   DROP TABLE IF EXISTS task_unit_configs;
   ```

3. **重启服务**
   - 服务启动时会自动加载配置到 Redis

4. **验证**
   - 检查 Redis 中是否有配置：`KEYS task:config:*`
   - 测试任务启动和进度查询

---

## 📚 相关文档

- [数据库设计](./数据库设计.md) - 需要更新
- [后端架构](./后端架构.md) - 需要更新
- [API接口设计](./API接口设计.md) - 需要更新

---

**更新日志**：
- 2024-XX-XX：创建文档，说明重构后的架构设计
