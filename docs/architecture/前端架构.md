# 前端架构

## 文档说明

本文档描述 DataTrace 前端（Vue 3 + TypeScript + Element Plus）的架构设计、目录结构和技术规范。

**文档版本**: v2.0
**最后更新**: 2026-07

---

## 技术栈

| 技术 | 版本 | 用途 |
|---|---|---|
| Vue 3 | 3.x | 前端框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 5.x | 构建工具 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.x | UI 组件库（按需引入） |
| Vue Router | 4.x | 路由管理 |
| EventSource | 原生 API | SSE 客户端 |

---

## 目录结构

```
datatrace-ui-designed/src/
├── main.ts                          # 应用入口
├── App.vue                          # 根组件（导航布局）
├── env.d.ts                         # 类型声明
├── router/
│   └── index.ts                     # 路由配置
├── stores/                          # Pinia 状态管理
│   ├── credential.ts                # 凭据 Store
│   ├── datasource.ts                # 数据源 Store
│   └── task.ts                      # 任务 Store（配置+监控统一）
├── api/                             # HTTP 请求封装
│   ├── credential.ts                # 凭据 API
│   ├── datasource.ts                # 数据源 API
│   └── task.ts                      # 任务 API
├── types/
│   └── index.ts                     # TypeScript 类型定义
├── utils/
│   └── dialog.ts                    # 对话框工具
├── assets/styles/
│   └── design-tokens.css            # 设计令牌（颜色/间距/字体）
├── components/                      # 通用组件
│   ├── Sidebar.vue                  # 侧边导航
│   ├── TopBar.vue                   # 顶部栏
│   ├── CredentialModal/
│   │   └── CredentialModal.vue      # 凭据新建/编辑弹窗
│   ├── DataSourceModal/
│   │   └── DataSourceModal.vue      # 数据源新建/编辑弹窗
│   └── TaskWizard/
│       └── TaskWizard.vue           # 4步任务配置向导
└── views/                           # 页面视图
    ├── Overview/
    │   └── OverviewView.vue         # 首页概览
    ├── DataSource/
    │   └── DataSourceView.vue       # 数据源管理
    ├── Credential/
    │   └── CredentialView.vue       # 凭据管理
    ├── TaskConfig/
    │   └── TaskConfigView.vue       # 任务配置
    └── TaskMonitor/
        └── TaskMonitorView.vue      # 任务监控
```

---

## 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    Views（页面视图）                      │
│  OverviewView / DataSourceView / CredentialView          │
│  TaskConfigView / TaskMonitorView                        │
└─────────────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────────────┐
│                 Components（组件）                        │
│  Sidebar / TopBar / CredentialModal / DataSourceModal    │
│  TaskWizard                                              │
└─────────────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────────────┐
│              Stores（Pinia 状态管理）                     │
│  useCredentialStore / useDatasourceStore / useTaskStore  │
│  - 全局状态管理 + SSE 连接管理                           │
└─────────────────────────────────────────────────────────┘
                        ↓ 调用
┌─────────────────────────────────────────────────────────┐
│                   API Layer（HTTP 请求）                  │
│  credential.ts / datasource.ts / task.ts                 │
│  - fetch/axios 封装                                      │
│  - 统一错误处理                                          │
└─────────────────────────────────────────────────────────┘
                        ↓ 调用
┌─────────────────────────────────────────────────────────┐
│              后端 REST API + SSE                          │
│  Gin HTTP 端点 + SSEHub 事件推送                         │
└─────────────────────────────────────────────────────────┘
```

---

## 路由结构

| 路径 | 页面 | 说明 |
|---|---|---|
| `/` | 首页 | 重定向到 `/task-monitor` |
| `/datasources` | DataSourceView | 数据源 CRUD |
| `/credentials` | CredentialView | 凭据 CRUD |
| `/task-config` | TaskConfigView | 任务列表 + 配置向导 |
| `/task-monitor` | TaskMonitorView | 任务列表 + 实时监控 |

---

## Pinia Store 设计

### useCredentialStore

```typescript
// stores/credential.ts
export const useCredentialStore = defineStore('credential', () => {
    const credentials = ref<Credential[]>([])
    const loading = ref(false)

    async function fetchCredentials() { ... }
    async function createCredential(data) { ... }
    async function updateCredential(id, data) { ... }
    async function deleteCredential(id) { ... }

    return { credentials, loading, fetchCredentials, createCredential, ... }
})
```

### useDatasourceStore

```typescript
// stores/datasource.ts
export const useDatasourceStore = defineStore('datasource', () => {
    const datasources = ref<DataSource[]>([])
    const healthStatus = ref<Record<string, HealthStatus>>({})

    async function fetchDataSources() { ... }
    async function testConnection(id) { ... }
    function updateHealthStatus(status) { ... }  // SSE 回调更新

    return { datasources, healthStatus, fetchDataSources, ... }
})
```

### useTaskStore

```typescript
// stores/task.ts
export const useTaskStore = defineStore('task', () => {
    const tasks = ref<Task[]>([])
    const eventSource = ref<EventSource | null>(null)
    const taskDetail = ref<TaskDetail | null>(null)
    const progress = ref<ProgressData | null>(null)
    const logs = ref<LogEntry[]>([])
    const tableStatus = ref<Record<string, string>>({})
    const stepStatus = ref<Record<string, string>>({})

    function connectSSE(taskId: string) { ... }
    function disconnectSSE() { ... }

    return { tasks, taskDetail, progress, logs, connectSSE, ... }
})
```

---

## SSE 连接管理

```typescript
function connectSSE(taskId: string) {
    const topics = [
        `task:${taskId}:detail`,
        `task:${taskId}:progress`,
        `task:${taskId}:logs`,
        `task:${taskId}:table_status`,
        `task:${taskId}:step_status`,
    ].join(',')

    const es = new EventSource(`/api/v1/sse?topics=${topics}`)

    es.addEventListener('task_detail', (e) => {
        taskDetail.value = JSON.parse(e.data)
    })
    es.addEventListener('progress', (e) => {
        progress.value = JSON.parse(e.data)
    })
    es.addEventListener('log', (e) => {
        logs.value.push(JSON.parse(e.data))
        if (logs.value.length > 1000) logs.value.shift()
    })
    es.addEventListener('table_status', (e) => {
        const data = JSON.parse(e.data)
        tableStatus.value[data.target_table] = data.status
    })
    es.addEventListener('step_status', (e) => {
        const data = JSON.parse(e.data)
        stepStatus.value[data.step] = data.status
    })

    eventSource.value = es
}
```

**生命周期**：
- 页面进入 → 建立 SSE 连接
- 页面离开 → 关闭连接 + 清除状态
- 新连接立即收到缓存快照（最新状态）

---

## 样式体系

- 设计令牌定义在 `assets/styles/design-tokens.css`
- 使用 CSS 变量统一管理颜色、间距、字体
- Element Plus 按需引入，减少 CSS 体积
- 组件使用 `<style scoped>` 避免样式冲突

---

## 交互规范

| 场景 | 表现 |
|---|---|
| 成功操作 | Toast 绿色提示，3s 自动关闭 |
| 错误操作 | Toast 红色提示，5s 可手动关闭 |
| 按钮操作 | loading 状态，禁用点击 |
| 删除确认 | MessageBox 弹窗确认 |
| 列表加载 | 骨架屏/Loading |
| 弹窗确认 | Enter 键触发确认 |
| 弹窗取消 | Esc 键关闭弹窗 |
# 前端架构

## 📋 文档说明

本文档详细描述 DataTrac 前端的架构设计、目录结构和技术规范。

**文档版本**: v1.0  
**最后更新**: 2026-01-30

---

## 🎯 技术栈

### 核心框架
- **Vue 3.4+** - 渐进式 JavaScript 框架
- **TypeScript 5.0+** - 类型安全
- **Vite 5.0+** - 构建工具

### UI 框架
- **Naive UI** - Vue 3 组件库
- **@vicons** - 图标库

### 状态管理
- **Pinia** - Vue 3 官方状态管理库

### 路由
- **Vue Router 4** - 官方路由库

### 通信
- **@tauri-apps/api** - Tauri IPC 通信

### 工具库
- **dayjs** - 日期处理
- **lodash-es** - 工具函数

---

## 📁 目录结构

```
src/
├── main.ts                     # 应用入口
├── App.vue                     # 根组件
├── vite-env.d.ts              # Vite 类型声明
├── router/                     # 路由配置
│   └── index.ts
├── stores/                     # Pinia 状态管理
│   ├── index.ts               # Store 入口
│   ├── datasource.ts          # 数据源 Store
│   ├── task.ts                # 任务 Store
│   └── monitor.ts             # 监控 Store
├── api/                        # API 调用层
│   ├── datasource.ts          # 数据源 API
│   ├── task.ts                # 任务 API
│   └── monitor.ts             # 监控 API
├── types/                      # 类型定义
│   └── index.ts               # 统一导出
├── utils/                      # 工具函数
│   ├── message.ts             # 消息提示
│   ├── format.ts              # 格式化函数
│   └── validate.ts            # 验证函数
├── components/                 # 组件
│   ├── common/                # 通用组件
│   │   ├── Button.vue
│   │   ├── Dialog.vue
│   │   └── Loading.vue
│   └── SyncTaskWizard/        # 任务配置向导
│       ├── index.vue          # 主组件
│       ├── TaskHeader.vue     # 任务头部
│       ├── StepIndicator.vue  # 步骤指示器
│       ├── Step1SelectDataSourceType.vue
│       ├── Step2SelectDataSource.vue
│       ├── Step3SelectUnits.vue
│       ├── Step4SyncConfig.vue
│       ├── Step5Confirm.vue
│       └── shared/            # 共享组件
│           ├── DataSourceSelector.vue
│           └── SyncConfigForm.vue
├── views/                      # 页面视图
│   ├── DataSourceManagement.vue  # 数据源管理
│   ├── SyncTaskConfig.vue        # 任务配置
│   ├── TaskMonitor.vue           # 任务监控
│   └── TaskMonitor/              # 任务监控子组件
│       ├── components/
│       │   ├── TaskList.vue      # 任务单元列表
│       │   ├── ProgressPanel.vue # 进度面板
│       │   ├── LogPanel.vue      # 日志面板
│       │   └── TaskControl.vue   # 任务控制
│       └── composables/
│           └── useTaskMonitor.ts # 监控逻辑
└── assets/                     # 静态资源
    ├── styles/                # 样式文件
    │   ├── main.css          # 全局样式
    │   └── variables.css     # CSS 变量
    └── images/               # 图片资源
```

---

## 🏗️ 分层架构

### 架构图

```
┌─────────────────────────────────────────────────────────┐
│                    Views (页面视图)                      │
│  - 页面级别的 UI 渲染                                    │
│  - 组织页面布局                                          │
│  - 调用 Composables 和 Stores                           │
└─────────────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────────────┐
│                 Components (组件)                        │
│  - 可复用的 UI 组件                                      │
│  - 接收 props，触发 events                              │
│  - 无状态或局部状态                                      │
└─────────────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────────────┐
│              Composables (组合式函数)                    │
│  - 封装业务逻辑                                          │
│  - 可复用的状态逻辑                                      │
│  - 调用 Stores 和 API                                   │
└─────────────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────────────┐
│                  Stores (状态管理)                       │
│  - 全局状态管理                                          │
│  - 缓存数据                                              │
│  - 调用 API 层                                          │
└─────────────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────────────┐
│                   API Layer (API 层)                     │
│  - 封装 Tauri Command 调用                              │
│  - 类型安全的接口                                        │
│  - 错误处理                                              │
└─────────────────────────────────────────────────────────┘
                        ↓ 调用
┌─────────────────────────────────────────────────────────┐
│                Tauri IPC (后端通信)                      │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 模块设计

### 1. API 层

**职责**:
- 封装所有 Tauri Command 调用
- 提供类型安全的接口
- 统一错误处理

**示例**:
```typescript
// src/api/datasource.ts
import { invoke } from '@tauri-apps/api/core';
import type { DataSource, CreateDataSourceRequest } from '../types';

export const datasourceApi = {
  async list(): Promise<DataSource[]> {
    try {
      return await invoke('list_data_sources');
    } catch (error) {
      console.error('Failed to list data sources:', error);
      throw error;
    }
  },
  
  async create(data: CreateDataSourceRequest): Promise<DataSource> {
    return await invoke('create_data_source', { data });
  },
  
  // ... 其他方法
};
```

### 2. Store 层

**职责**:
- 管理全局状态
- 缓存数据
- 提供状态操作方法

**示例**:
```typescript
// src/stores/datasource.ts
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import { datasourceApi } from '../api/datasource';

export const useDataSourceStore = defineStore('datasource', () => {
  // 状态
  const dataSources = ref<DataSource[]>([]);
  const loading = ref(false);
  const error = ref<string | null>(null);
  
  // 计算属性
  const mysqlDataSources = computed(() => 
    dataSources.value.filter(ds => ds.type === 'mysql')
  );
  
  // 方法
  async function fetchDataSources() {
    loading.value = true;
    error.value = null;
    try {
      dataSources.value = await datasourceApi.list();
    } catch (e: any) {
      error.value = e.message;
      throw e;
    } finally {
      loading.value = false;
    }
  }
  
  return {
    dataSources,
    loading,
    error,
    mysqlDataSources,
    fetchDataSources,
  };
});
```

### 3. Composables 层

**职责**:
- 封装可复用的业务逻辑
- 组合多个 Store
- 处理复杂的状态逻辑

**示例**:
```typescript
// src/views/TaskMonitor/composables/useTaskMonitor.ts
import { ref, onMounted, onUnmounted } from 'vue';
import { useMonitorStore } from '@/stores/monitor';
import { listen } from '@tauri-apps/api/event';

export function useTaskMonitor(taskId: string) {
  const monitorStore = useMonitorStore();
  const progress = ref<TaskProgress | null>(null);
  let unlisten: (() => void) | null = null;
  
  // 启动监听
  async function startListening() {
    unlisten = await listen<TaskProgress>('task-progress', (event) => {
      if (event.payload.taskId === taskId) {
        progress.value = event.payload;
      }
    });
  }
  
  // 停止监听
  function stopListening() {
    if (unlisten) {
      unlisten();
      unlisten = null;
    }
  }
  
  onMounted(() => {
    startListening();
  });
  
  onUnmounted(() => {
    stopListening();
  });
  
  return {
    progress,
    startTask: () => monitorStore.startTask(taskId),
    pauseTask: () => monitorStore.pauseTask(taskId),
    stopTask: () => monitorStore.stopTask(taskId),
  };
}
```

### 4. 组件层

**职责**:
- 可复用的 UI 组件
- 接收 props，触发 events
- 无状态或局部状态

**组件分类**:

**通用组件** (`components/common/`):
- 按钮、输入框、对话框等
- 完全无业务逻辑
- 高度可复用

**业务组件** (`components/`):
- 包含特定业务逻辑
- 可能调用 Store 或 API
- 在特定场景使用

**示例**:
```vue
<!-- src/components/common/Button.vue -->
<template>
  <button
    :class="['custom-button', `custom-button--${type}`]"
    :disabled="disabled || loading"
    @click="handleClick"
  >
    <span v-if="loading" class="loading-icon">⏳</span>
    <slot />
  </button>
</template>

<script setup lang="ts">
interface Props {
  type?: 'primary' | 'default' | 'danger';
  disabled?: boolean;
  loading?: boolean;
}

const props = withDefaults(defineProps<Props>(), {
  type: 'default',
  disabled: false,
  loading: false,
});

const emit = defineEmits<{
  click: [];
}>();

function handleClick() {
  if (!props.disabled && !props.loading) {
    emit('click');
  }
}
</script>
```

### 5. 页面层

**职责**:
- 页面级别的 UI 渲染
- 组织页面布局
- 调用 Composables 和 Stores

**示例**:
```vue
<!-- src/views/TaskMonitor.vue -->
<template>
  <div class="task-monitor">
    <TaskControl
      :task-id="taskId"
      :status="progress?.status"
      @start="handleStart"
      @pause="handlePause"
      @stop="handleStop"
    />
    
    <ProgressPanel
      :progress="progress"
      :task-units="taskUnits"
    />
    
    <LogPanel
      :logs="logs"
    />
  </div>
</template>

<script setup lang="ts">
import { useRoute } from 'vue-router';
import { useTaskMonitor } from './composables/useTaskMonitor';
import TaskControl from './components/TaskControl.vue';
import ProgressPanel from './components/ProgressPanel.vue';
import LogPanel from './components/LogPanel.vue';

const route = useRoute();
const taskId = route.params.id as string;

const {
  progress,
  taskUnits,
  logs,
  startTask,
  pauseTask,
  stopTask,
} = useTaskMonitor(taskId);

async function handleStart() {
  await startTask();
}

async function handlePause() {
  await pauseTask();
}

async function handleStop() {
  await stopTask();
}
</script>
```

---

## 🎨 样式组织

### CSS 变量

```css
/* src/assets/styles/variables.css */
:root {
  /* 颜色 */
  --color-primary: #18a058;
  --color-success: #18a058;
  --color-warning: #f0a020;
  --color-error: #d03050;
  --color-info: #2080f0;
  
  /* 间距 */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  
  /* 字体 */
  --font-size-sm: 12px;
  --font-size-md: 14px;
  --font-size-lg: 16px;
  --font-size-xl: 18px;
  
  /* 圆角 */
  --border-radius-sm: 2px;
  --border-radius-md: 4px;
  --border-radius-lg: 8px;
}
```

### 样式规范

1. **使用 scoped 样式**
   ```vue
   <style scoped>
   .component { ... }
   </style>
   ```

2. **使用 CSS 变量**
   ```css
   .button {
     padding: var(--spacing-md);
     border-radius: var(--border-radius-md);
   }
   ```

3. **BEM 命名规范**
   ```css
   .block { }
   .block__element { }
   .block--modifier { }
   ```

---

## 🔧 工具函数

### 消息提示

```typescript
// src/utils/message.ts
import { useMessage } from 'naive-ui';

let message: ReturnType<typeof useMessage> | null = null;

export function setupMessage() {
  message = useMessage();
}

export function showSuccess(text: string) {
  message?.success(text);
}

export function showError(text: string) {
  message?.error(text);
}

export function showWarning(text: string) {
  message?.warning(text);
}
```

### 格式化函数

```typescript
// src/utils/format.ts
import dayjs from 'dayjs';

export function formatDate(date: string | number): string {
  return dayjs(date).format('YYYY-MM-DD HH:mm:ss');
}

export function formatFileSize(bytes: number): string {
  if (bytes === 0) return '0 B';
  const k = 1024;
  const sizes = ['B', 'KB', 'MB', 'GB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return `${(bytes / Math.pow(k, i)).toFixed(2)} ${sizes[i]}`;
}

export function formatNumber(num: number): string {
  return num.toLocaleString('zh-CN');
}
```

---

## 📚 相关文档

- [技术规范](./技术规范.md) - 前后端统一规范
- [API接口设计](./API接口设计.md) - API 接口文档
- [UI设计](../design/UI设计.md) - UI 设计规范
- [交互设计](../design/交互设计.md) - 交互流程

---

**文档维护**: DataTrac 开发团队  
**最后更新**: 2026-01-30
