# 设计概览

## 文档说明

本文档提供 DataTrace 系统的设计概览，帮助快速理解系统全貌。

**文档版本**: v2.0
**最后更新**: 2026-07

---

## 系统定位

DataTrace 是一个 **MySQL 数据同步工具**，支持：
- 全量同步：多表并发、分批读写、拓扑排序
- 增量同步：Binlog 监听、事件队列、断点续传
- 多目标：一个源 → N 个目标

---

## 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│  前端 (Vue 3 + Element Plus + Pinia)                        │
│  5 个页面 + SSE 实时推送                                     │
└─────────────────────────────────────────────────────────────┘
                    ↕ HTTP REST + SSE
┌─────────────────────────────────────────────────────────────┐
│  后端 (Go + Gin)                                             │
│                                                              │
│  API Layer ──→ Service Layer ──→ Infrastructure              │
│  (路由+Handler)  (业务逻辑)       (MySQL + Redis)             │
│                                                              │
│  横切：SSEHub + EventBus + CacheStore                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 核心模块

### 1. 基础管理

| 模块 | 职责 |
|---|---|
| 凭据管理 | 数据库连接凭据 CRUD + AES 加密 |
| 数据源管理 | 数据源 CRUD + 连接测试 + 健康检查 |
| 任务管理 | 任务 CRUD + 配置管理 + 统计 |

### 2. 全量同步

```
TaskControlService.StartFullSyncTask()
  → ForeignKeyAnalyzer → 拓扑排序
  → SyncEngine.Worker() × N 并发
    → MySQLReader.ReadBatch() → MySQLWriter.BatchInsert()
    → EventBus.Publish("task.progress") → SSEHub 推送
```

### 3. 增量同步

```
TaskControlService.StartIncrementalTask()
  → FullSyncStarter → 先全量
  → BinlogListener → BinlogQueue × N（扇出）
  → IncrementalConsumer × N → MySQLWriter 写入目标
  → IncrementalStatsService → SSE 推送统计
```

### 4. 实时监控

```
EventBus.Publish() → SSEHub 桥接 → SSE 推送 → 前端 EventSource
```

7 个事件类型：
- `task:{id}:progress` — 进度
- `task:{id}:detail` — 状态
- `task:{id}:logs` — 日志
- `task:{id}:table_status` — 表状态
- `task:{id}:step_status` — 步骤状态
- `task:{id}:incremental` — 增量统计
- `health:all` — 健康检查

---

## 数据模型

| 表 | 说明 |
|---|---|
| `credentials` | 凭据（用户名+加密密码） |
| `data_sources` | 数据源（连接信息） |
| `sync_tasks` | 同步任务（配置+状态+Binlog 位点） |
| `task_target_sources` | 任务-目标关联（多目标） |

---

## 前端页面

| 页面 | 路由 | 说明 |
|---|---|---|
| OverviewView | `/` | 首页概览（重定向到监控） |
| DataSourceView | `/datasources` | 数据源管理 |
| CredentialView | `/credentials` | 凭据管理 |
| TaskConfigView | `/task-config` | 任务配置（4步向导） |
| TaskMonitorView | `/task-monitor` | 任务监控（实时进度+日志） |

---

## 关键设计决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 依赖注入 | AppContainer | 统一管理生命周期，禁止全局变量 |
| 推送机制 | SSE + EventBus | 实时推送，禁止轮询 |
| 缓存策略 | Memory/Redis 降级 | Redis 可选，保证基础功能 |
| 队列策略 | 内存/Redis 队列 | 内存简单快速，Redis 持久化 |
| 加密方式 | AES-256-GCM | 安全对称加密 |
| 多目标 | 扇出模式 | 一个 Binlog → N 个 Queue → N 个 Consumer |
# DataTrace 设计概览

> 更新时间：2024年（重构后）
> 
> MySQL数据同步系统 - 简化架构版本

## 🎯 系统目标

构建一个高性能、易维护的MySQL数据同步系统，支持：
- ✅ 全量同步（快速、内存化）
- ✅ 增量同步（基于Binlog）
- ✅ 实时监控（SSE推送）
- ✅ 灵活配置（表映射、字段映射）

---

## 🏗️ 系统架构

### 技术栈

**后端**:
- Go 1.21+
- Gin (Web框架)
- GORM (ORM)
- go-mysql (Binlog解析)
- Redis (配置缓存 + 队列)

**前端**:
- 原生 JavaScript
- SSE (Server-Sent Events)
- 模块化设计

**存储**:
- MySQL (核心配置)
- Redis (详情配置 + 统计)
- 内存 (运行时进度)

---

## 📊 数据存储架构

```
┌─────────────────────────────────────────────────────────┐
│                      数据存储层                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  MySQL                Redis               内存          │
│  ├─ data_sources     ├─ task:config:*    ├─ 进度数据   │
│  └─ sync_tasks       └─ task:stats:*     └─ 临时状态   │
│                                                         │
│  持久化核心配置       缓存详情配置         运行时数据    │
│                      增量统计                           │
└─────────────────────────────────────────────────────────┘
```

### 存储职责

| 存储 | 内容 | 特点 |
|------|------|------|
| MySQL | 数据源、任务基本信息、最终状态 | 持久化、ACID |
| Redis | 表映射配置、增量统计 | 高性能、可持久化 |
| 内存 | 全量同步进度 | 极速、临时 |

---

## 🔄 同步模式

### 1. 全量同步模式

**流程**:
```
启动 → Redis读配置 → 内存进度 → 多Worker并发 → SSE推送 → 完成清空
```

**特点**:
- 完全在内存中维护进度
- 无数据库IO，性能极高
- 支持多线程并发
- 支持外键依赖排序
- 完成后清空内存

**适用场景**:
- 首次数据迁移
- 定期全量备份
- 数据重建

---

### 2. 增量同步模式

**流程**:
```
启动 → 捕获快照点
  ├─ 线程1: Binlog监听 → Redis队列（持续）
  └─ 线程2: 全量同步 → 内存进度 → 完成
       ↓
  增量消费 → Redis队列 → 目标库 → Redis统计 → SSE推送
```

**特点**:
- 双线程并行（全量 + Binlog监听）
- 全量阶段复用全量同步逻辑
- 增量阶段消费Redis队列
- 统计数据持久化到Redis

**适用场景**:
- 实时数据同步
- 数据库迁移后的持续同步
- 灾备系统

---

## 🎨 功能模块

### 1. 数据源管理

**功能**:
- 数据源CRUD
- 连接测试
- 密码加密存储

**支持类型**:
- MySQL
- Elasticsearch（预留）

---

### 2. 任务配置

**配置向导**:
1. 选择数据源（源 + 目标）
2. 选择数据库和表
3. 配置表映射（可选）
4. 配置同步参数

**配置项**:
- 批次大小（batch_size）
- 线程数（thread_count）
- 同步模式（full/incremental）
- 错误策略（pause/skip）
- 表存在策略（drop/truncate/append）

---

### 3. 任务监控

**实时监控**:
- SSE 实时推送进度
- 总体进度条
- 表级进度展示
- 分类日志展示

**全量同步展示**:
```json
{
  "overall_progress": 45.5,
  "sync_speed": 1000,
  "total_tables": 10,
  "completed_tables": 4,
  "total_records": 100000,
  "processed_records": 45500
}
```

**增量同步展示**:
```json
{
  "table_stats": [
    {
      "table_name": "users",
      "insert_count": 100,
      "update_count": 50,
      "delete_count": 10
    }
  ]
}
```

---

### 4. 任务控制

**操作**:
- 启动任务
- 暂停任务（仅全量）
- 停止任务
- 删除任务

**状态管理**:
- idle（未配置）
- configured（已配置）
- running（运行中）
- completed（已完成）

---

## 🔧 核心组件

### 1. ConfigCacheService

**职责**: Redis配置缓存管理

**方法**:
- `LoadTaskConfigToRedis()` - 加载配置到Redis
- `GetTaskConfigFromRedis()` - 从Redis读取配置
- `DeleteTaskConfigFromRedis()` - 删除Redis配置
- `InitAllTaskConfigs()` - 服务启动时初始化

---

### 2. TaskProgressManager

**职责**: 内存进度管理

**方法**:
- `InitTask()` - 初始化任务进度
- `UpdateUnitStatus()` - 更新单元状态
- `UpdateUnitProgress()` - 更新单元进度
- `GetTask()` - 获取任务进度
- `ClearTask()` - 清空任务进度

---

### 3. SyncEngine

**职责**: 全量同步引擎

**方法**:
- `InitializeWorker()` - 初始化阶段（创建表结构）
- `Worker()` - 数据同步Worker
- `SyncTable()` - 同步单个表

**特点**:
- 支持多Worker并发
- 支持外键依赖排序
- 进度实时更新到内存

---

### 4. IncrementalSync

**职责**: 增量同步总控

**组件**:
- BinlogListener - Binlog监听器
- BinlogQueue - 事件队列
- IncrementalConsumer - 增量消费者

**流程**:
1. 捕获快照点
2. 启动Binlog监听
3. 执行全量同步
4. 启动增量消费

---

## 📡 SSE 推送机制

### 推送端点

- `/api/tasks/:id/progress/stream` - 进度推送
- `/api/tasks/:id/logs/stream` - 日志推送
- `/api/tasks/:id/detail/stream` - 详情推送

### 推送频率

- 进度更新：每批次
- 日志推送：实时
- 详情更新：状态变化时

---

## 🎯 性能优化

### 1. 内存化进度

- 全量同步进度完全在内存
- 无数据库IO
- 极速更新

### 2. 多线程并发

- 支持配置Worker数量
- 外键表串行，普通表并发
- 充分利用CPU

### 3. 批量操作

- 批量读取数据
- 批量写入数据
- 减少网络往返

### 4. Redis缓存

- 配置缓存到Redis
- 减少MySQL查询
- 提高响应速度

---

## 🔒 安全设计

### 1. 密码加密

- AES-256-GCM 加密
- 32字节密钥
- 存储加密后的密码

### 2. 连接安全

- 连接超时控制
- 连接池管理
- 错误重试机制

---

## 📈 扩展性

### 1. 数据源扩展

- 接口化设计
- 易于添加新数据源类型
- 当前支持：MySQL
- 预留：Elasticsearch、PostgreSQL

### 2. 队列扩展

- 支持内存队列
- 支持Redis队列
- 易于扩展其他队列

### 3. 监控扩展

- SSE推送机制
- 易于添加新的监控指标
- 支持自定义日志分类

---

## 🚀 部署建议

### 最小配置

- CPU: 2核
- 内存: 4GB
- MySQL: 5.7+
- Redis: 5.0+

### 推荐配置

- CPU: 4核+
- 内存: 8GB+
- MySQL: 8.0+
- Redis: 6.0+（开启持久化）

---

## 📚 相关文档

- [数据库设计](./数据库设计.md)
- [重构后架构说明](./architecture/重构后架构说明.md)
- [API接口设计](./architecture/API接口设计.md)
- [前端架构](./architecture/前端架构.md)
